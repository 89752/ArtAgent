# 2026-08-01 工作日志

## Stage 2：RAG 抽象层实现（ArtAgent 2.0）

按 `docs/ArtAgent-2.0-实施方案.md` §3 完成 Stage 2（继 Stage 1 commits a08bda1/1aaf48c 之后）。

**新建 `src/retrieval/` 包：**
- `base.py`：RetrievalResult（source 枚举预留 met_museum/rijksmuseum）+ BaseRetriever Protocol
- `structured_retriever.py`：TableSchema（supports_timeline/supports_recommendation 自动推出）+ SEMART_SCHEMA + StructuredTableRetriever（group_by_axis / exclude_by_entity / exclude_from_results / search 双路径）+ 数据源注册表（SemArt 懒注册 dataset_id="semart"）
- `hybrid.py`：HybridRetriever（扇出→RRF k=60 融合→page_id/doc_id 去重）；BGE/Chroma 单例从 tools/retrieval.py 迁入

**集成点：** AgentState.dataset_id；graph.py 路由能力开关（只读 schema 零成本）；timeline/recommendation 节点改走 StructuredTableRetriever；semantic_search 改走 HybridRetriever（返回形状不变）；service.py 重置清单同步。

**关键决策（已写入实施方案 §1.4）：**
1. 注册全懒加载（schema + loader），能力开关零成本
2. recommendation 实际用 exclude_from_results（排除发生在结果字典上，非 DataFrame）
3. RRF 只定序不改分，单源严格保序 → Recall@5 应逐位持平
4. 去重只作用于带 page_id/doc_id 的结果（SemArt 行不参与）

**验收（全绿）：**
- 纯单测 53 个：structured_retriever 23 + hybrid 13 + access 17
- test_tools / test_pipelines / test_multi_tool 全绿
- eval 意图分类 98.0%（基线 96.0%）
- Recall@5：新旧路径 25 条 query 逐位 0 不一致（tests/verify_recall_parity.py）；n=25 复现基线恰 64.0%。**重要口径发现：文档基线 64.0% 来自 n=25；run_eval.py 默认 n=20 跑出 70.0%，同 seed 不同样本量，非回归**。已把口径写进实施方案 §0/§3.3/§1.4。

**收尾：** 实施方案文档 §1.4 Stage 2 实施记录已写入（含 5 条关键决策 + 验收数据）；§13 陷阱速查新增 3 条（单例迁移、注册懒加载、RRF 只信排名）。

---

## Stage 3：PDF 解析与入库（同日，用户指令"做 stage3"）

**新建 `src/ingestion/` 包**：page_classifier（PyMuPDF 信号采集+确定性路由 text/multimodal/dual+force_mineru 标记）、blocks（Block/Chunk）、pdfplumber_fallback（兜底解析，MinerU 可插拔接口）、chunker（按 block_type 归并/拆分）、multimodal_indexer（整页渲染 150DPI → DashScope tongyi-embedding-vision-plus 实测 1152 维文图同空间 → user_pdf_images collection）、pipeline（编排 + JSON 状态存储）。

**检索层扩展**：UserDocTextRetriever（user_pdf_text，BGE 空间）+ UserDocImageRetriever 注册进 HybridRetriever；_dedup 改路线感知（同页文字 chunk 胜整页图，多文字 chunk 全保留）；semantic_search 按 source 分形状（PDF→《doc》第N页 title）；collect_artworks/_parse_artworks 跳过带 source 键的片段防配图污染。

**Web 链路**：POST /api/documents/upload（BackgroundTasks 后台解析）+ GET /api/documents 轮询；前端侧栏文档面板（上传+状态徽标 3s 轮询）。新依赖 PyMuPDF/pdfplumber/dashscope/python-multipart 已入 requirements。

**E2E 验证**：百度百科 RAG PDF（4文字+1双路线→21 chunks+1图）+ Renaissance PDF（3双路线→6 chunks+3图）；混合检索来源标签正确；双路线去重正确（文字命中抑制整页图）；回答正确声明"上传文档讨论的是 RAG 技术"。

**关键决策/坑（已写入方案 §1.5/§13）：**
1. MinerU 未接入（4GB 模型+datasets 冲突风险），pdfplumber 先跑通全链路，接口可插拔——遗留
2. Qwen-VL 读图作答未接入（ToolMessage 多模态化改 ReAct 循环）——遗留
3. eval/parity 必须锁 sources=["semart"] 防用户 PDF 污染 64.0% 基线口径
4. 级联删除验证 OK：coll.get(where={"doc_id"})→delete(ids)；两 collection 各清一遍
5. 沙箱 safe-delete 拦截 rmtree/os.remove：data/uploads/default/ 下测试目录残留需用户手动删（gitignored 无害）
6. Git Bash curl 发中文 JSON 会变 GBK 导致 500：用 Python 写 UTF-8 文件 + --data-binary
7. FastAPI UploadFile 需要 python-multipart（报错才暴露）
8. Git Bash /tmp 与 Python /tmp 路径不同：cygpath -w 转换

**验收**：纯单测累计 81 个全绿；verify_recall_parity 锁源后 25 条 0 不一致；test_pipelines 5 问全过、test_tools 4 工具全过。

**重大事故记录（超时与额度）：**
- 全量回归两次在 `rec_filter` 节点卡死（一次 33 分钟无响应）——根因是 DashScope 偶发连接挂起 + LLM 客户端 `request_timeout=None` 无限等待。修复：`src/utils/llm.py` 和 `image_lookup.py` 加 `request_timeout=180` + `max_retries=2`（修复后同一调用 161s 正常跑完）。**教训：任何外部 API 客户端必须显式设超时。**
- 随后 eval 第 38/50 条撞上 **403 Free quota exhausted（FreeTierOnly）**——当天上午 4 轮全量测试（每轮 60+ 调用）耗尽 DashScope 免费额度。之前的挂起很可能就是耗尽前限流前兆。

**模型切换（当日下午，用户提供 GLM 额度截图后）：**
- 发现百炼 GLM 系列模型各有**独立** 100 万 token 免费额度（glm-4.5/4.6/4.7）。实测：glm-4.7/4.6 对话+工具调用均正常；glm-4.5 仅支持 stream 模式不可用。
- 原模型其实是 **glm-5**（不是 deepseek-v3，DEEPSEEK_MODEL 变量名早已名不符实），其额度被耗尽。已将 `.env` 的 `DEEPSEEK_MODEL` 切为 **glm-4.7**（glm-4.6 作后备）。
- **eval 补跑完成（glm-4.7）**：意图分类 **96.0%**（48/50，基线 96.0% 达标）；Recall@5 n=20 口径 70.0%（锁 semart 源）。glm-4.7 比 glm-5 意图分类快约 40%。
- 结论：**以后额度再耗尽，先查百炼控制台还有哪些模型有独立免费额度，改 DEEPSEEK_MODEL 即可换血**。

---

## Qwen-VL 读图闭环（Stage 3 遗留项①，同日完成）

- 新建 `src/tools/page_reader.py`：`read_page_image(image_path, question)`——路径安全校验（只放行 data/uploads 下图片，防穿越）+ qwen3.5-omni-plus 读图返回文字描述。注册为第 6 个 GENERAL_TOOL。
- **关键架构分工**：glm-4.7 是纯文本大脑（工具决策），qwen3.5-omni-plus 是唯一"眼睛"（image_lookup analyze / read_page_image）。视觉客户端统一收敛到 `utils/llm.py::get_vision_llm`（image_lookup 已复用，删除其私有副本）。
- SYSTEM_PROMPT 更新（工具清单 5→6、user_pdf 结果引用规范）；semantic_search 整页图结果加 `read_hint` 字段提示 Agent 可调 read_page_image。
- E2E 验证：构造 2 页测试画册（文字页+纯图版页《星夜》），Agent 依次调 semantic_search → read_page_image → 准确答出画面内容（旋转星空/柏树/村庄/厚涂）。测试后向量已级联清理。
- 纯单测累计 **89 个**全绿（新增 page_reader 8 个：路径校验+错误分支；测试清理改容错式应对沙箱 safe-delete 拦截）。
- 遗留项只剩 MinerU 接入（等真实画册 PDF 验证）。

---

## MinerU 接入前置讨论（用户提供"莫奈手稿"PDF 后）

- 用户提供的《提取自莫奈手稿》PDF 实测**只有 1 页纯图（书封）**，无文字层，无法用于 MinerU 验证——需要完整全书（几百页、含正文+图版混排页）。
- 厘清双路线分工：多模态路线管"无文字层页"；MinerU 的价值在**文字/双路线页的版面解析质量**（多栏、图注、标题结构），其官方短板恰是画册版式——验证目标是"同页面对比 MinerU vs pdfplumber 谁更好"，不是"MinerU 读图"。
- 环境隔离决策：**独立 venv（~/.workbuddy/envs/mineru）+ 子进程调 mineru CLI**，主环境 artagent 零 MinerU 依赖，隔离 DLL 冲突/崩溃风险；MinerU 不可用仍优雅降级 pdfplumber。首次需下 ~4GB 模型（modelscope 源）+ torch。
- 待办：用户提供完整画册 PDF → 装 MinerU → 同页质量对比 → 决定接入与否。

---

## MinerU API 验证（同日，用户重新提供 16 页提取版画册）

- 用户提供的 16 页提取版《莫奈手稿》（5.4MB）实测为**全扫描件**（16 页全部 0 文字层、图占比 100%）——恰好是 MinerU 官方短板的最硬核场景，且 pdfplumber 在其上提取量为零。
- **MinerU 在线 API 验证通过且质量优秀**：用免 Token 的 Agent 轻量解析 API（`POST /api/v1/agent/parse/file` 签名上传 → PUT → 轮询 → Markdown，≤20页/≤10MB），脚本 `scripts/test_mineru_api.py`。16 页全 OCR 连贯、章节标题正确、图版标注 `<!-- image-->` 且图注完整提取（标题/作者/材质/年代/收藏地）、专名双语保留。**结论：画册短板不构阻碍，接入是净收益。**
- **接入路线修正（废弃本地 venv 方案）**：改用在线 API——零环境风险、零下载。**生产用精准解析 API**（需免费 Token，每日 2000 页高优先级额度；返回带 block_type/page_idx 的结构化 JSON，正是 chunker 所需）；轻量 API 只出 Markdown 无页码结构，仅适合验证/小文件。
- 精准解析 API 集成要点：`POST /api/v4/file-urls/batch` 申请签名 URL → PUT 上传 → `GET /api/v4/extract-results/batch/{batch_id}` 轮询 → 下载 zip 取 content_list.json。
- **待用户**：去 mineru.net/apiManage/token 申请免费 Token → 我写 `mineru_parser.py` 接入 pipeline（MinerU 优先、pdfplumber 兜底、扫描页仍走多模态）。
- 对照基准已存 `data/uploads/test-fixture/monet-album.mineru.md`。

---

## MinerU 路线再确认（同日中午，用户重新提问 API vs 本地）

- 用户对"官方 API 轻量解析 vs 本地安装"重新提问（担心大文件）。已澄清：生产路线是**精准解析 API v4**（200MB/600页，每日 2000 页高优额度，产出带 block_type/page_idx 的 content_list.json），不是轻量 API（≤20页/≤10MB 仅 Markdown，无页码结构，只适合验证）；本地安装维持否决（4GB 模型 + datasets/pyarrow DLL 冲突前科 + CPU 慢，且画册扫描件实测 API 质量优秀，本地无质量收益）。
- "大文件"担心拆解：应用自身 50MB 上传 cap 远低于 API 上限；上传链路（签名 URL → PUT）已实测通过；解析在云端，本地 BackgroundTasks 轮询天然适配；API 失败可降级 pdfplumber/多模态。
- 下一步不变：用户申请 mineru.net Token → 写 `src/ingestion/mineru_parser.py`（同签名 parse_pages，v4 流程：file-urls/batch → PUT → extract-results 轮询 → zip 取 content_list.json），`_mineru_available()` 改为探测 Token 配置。
- `.env` 已加 `MINERU_TOKEN=` 占位（含申请链接与降级说明注释），用户自行填写；.env 已确认在 .gitignore 中。

---

## MinerU 接入落地（同日下午，Stage 3 最后一块 ✅）

- 用户填好 Token 后完成接入。**新建 `src/ingestion/mineru_parser.py`**：v4 流程（file-urls/batch 申请签名 URL → PUT 自动开解析 → extract-results 轮询 → zip 取 `*_content_list.json` → Block）；`model_version=vlm`；整份上传按页过滤（page_ranges 的 page_idx 语义未明，Phase 2 优化点，quota 按整文档页数计）；块映射 text/list/code→text、equation、table（caption+HTML+footnote）、image/chart→文档图注（无图注不产块）、header/footer 等噪声丢弃；内嵌图落盘 work_dir/images/。
- **pipeline 接入**：`_mineru_available()` 改为探测 MINERU_TOKEN；新增 `_parse_text_route()`——MinerU 优先、调用失败降级 pdfplumber、公式密集页仍退多模态。requirements 补显式依赖 requests。
- **验收**：13 个新纯单测（fixture content_list，累计纯单测 94 个全绿）；真实 API 冒烟 RAG PDF 5 页 8.8s 解析 160 blocks；ingest_pdf e2e 52 chunks（含 1 table 块，table 路径首次真实命中）+ 1 整页图，级联清理已做；test_tools/test_pipelines/test_multi_tool 回归全绿（17.5 分钟）。
- **文档同步**：实施方案 §1.5 决策#1 改为已闭环、§4 标题去遗留项标注、§13 新增陷阱 #18（MinerU quota 按整文档计 + page_ranges 勿用）。
- **Stage 3 至此全部完成**，下一步 Stage 4（检索质量层：上下文头/qwen3-rerank/相关性校正通用化）。

---

## Stage 4：检索质量层（同日下午完成 ✅）

开工时发现：rerank 模块与 hybrid 集成、上下文头已由前序会话写好（未提交），缺纯单测与相关性校正通用化。本次补完剩余全部并验收。

**上下文头（前序已落地，本次确认）**：`pipeline.py::_context_header`——向量化拼 `《文档》 | 章节` 头，不改存储（header 落 metadata.context_header），展示侧章节并进标题；旧文档需 Stage 6 重解析补头。

**Rerank（补单测 + 修隐患）**：
- `src/retrieval/reranker.py`（30s 超时 + 2 重试 + 失败返 None 降级）+ `hybrid.py::_rerank_fused`（池 RERANK_POOL=40、整页图保持原槽位、rerank_score 写 metadata）。
- **修的隐患**：精排 API 部分响应（条数 < 候选数）按槽位重排会复制文档占两位——加守卫 `len(ranked) != len(text_slots)` 整体回退粗排原序。
- `tests/test_reranker.py` 15 个纯单测（mock requests）。

**相关性校正通用化（本次新建）**：
- `src/retrieval/relevance.py::llm_relevance_filter`：编号候选 → 确定性 LLM 选编号 → 只删不重排；失败返原列表；过度过滤按原序兜底补足 min_keep=2（永不返回空证据）；超 max_candidates=12 尾部透传。env `RELEVANCE_FILTER_ENABLED` 开关。
- 集成两处：`comparison_retrieve`（每对象过滤后再合成）；`general_tools` 节点（ToolNode 包成普通节点，对 semantic_search 的 ToolMessage 过滤后重序列化，query 取自 tool_call args；节点名不变 service.py 免同步）。
- **关键纪律**：semantic_search 工具保持无 LLM（eval/parity/test_tools 直接消费，必须确定性）；过滤只在编排层，日志可观测。recommendation 保留专用 rec_filter；timeline 无向量检索不适用。
- `tests/test_relevance.py` 15 个纯单测（fake LLM + stub ToolNode）。

**验收（2026-08-01，glm-4.7）**：
- 纯单测累计 **132 个全绿**。
- eval（锁 semart 源 n=25）：意图分类 **98.0%**；**Recall@5 = 76.0%（19/25），较 64.0% 基线 +12pp**——逐条 A/B：3 条救回、0 条回退，纯增益。
- parity（RERANK_ENABLED=0）25 条逐位 0 不一致、复现 64.0%；test_tools/test_multi_tool/test_pipelines 全绿（13 分钟）。
- **口径陷阱**：Recall@5 两口径——关精排 64.0%（基线）/ 开精排 76.0%；parity 必须关精排跑，否则"不一致"是设计行为非 bug（已写入 §13 #19/#20）。
- **回归变慢属正常**：每次 semantic_search = BGE + 40 候选 + rerank + glm 过滤，全量回归时长约 Stage 3 的 1.5–2 倍；怀疑卡死先看日志事件是否滚动（用户中途担心额度耗尽，实为 glm-4.7 响应慢 + Stage 4 新增调用）。

**提交**：commit 1 `001b456`（Stage 3 MinerU 收尾补提：mineru_parser + 13 单测 + 文档）；commit 2 `831f058`（Stage 4 全部 + 实施方案 §1.6）。

---

## Stage 5 前置：测试表格素材三件套定案（同日下午）

- 用户提议"正样本直接复用 SemArt 走上传链路"——确认可行且最优：**不要整库上传**（21k 行无意义且慢），Stage 5 开工时脚本导出分层子集 CSV（3–5 个跨多 TIMEFRAME 画家全作品 + 噪声行共 100–300 行）；AUTHOR 倒序格式顺带压测上传表的 fuzzy_match；Ground Truth 已知便于对照。
- 用户提供的学习计划 xlsx 当负样本**不合适**（实读结构：每日打卡表有 天数/日期/阶段 日期类列 + 学习主题/具体内容，LLM 几乎必推断为 supports_timeline/recommendation=True；且 4 子表、首表是 2 列说明页）。重新定位为**刁难样本**：专测 schema 人工纠正交互 + 多 sheet 选择。
- 负样本改为我生成的 `tests/fixtures/plain_list_books.csv`（已入库）：书名/作者/出版社/页数/价格——有实体列、无时间轴、无自由文本描述。
- 实施方案 §12 测试表格素材行已更新为三件套决策。
- **环境变化**：artagent 环境新装 openpyxl（纯 Python 无 DLL 风险），已写入 requirements.txt 标注 Stage 5 依赖。本地 editor_sdk MCP 读这份 xlsx 连续报 "No workbook open"（close+重开无效），按路由规则回退 pandas 读取——此类本地 xlsx 结构检查以后可直接 pandas。
