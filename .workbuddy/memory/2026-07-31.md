# 2026-07-31

## ArtAgent 2.0 设计方案评估（用户请求：评估能否按步骤开发）

对照真实代码逐条核验了 `Downloads/ArtAgent-2.0-设计方案.md`，结论：**可按步骤开发，但需先补 8 个遗漏点**。

### 核验通过的事实
- 7 工具中 6 个绑 SemArt（general.py GENERAL_TOOLS 确认）；模糊匹配 4 处重复（retrieval/image_lookup/style_comparison/image_analysis）；截断 200/250/300 三处不一；timeline_gather_periods 双重查询；compare_artwork_styles 与 query_painter_knowledge 内藏 LLM 调用——全部属实。
- 外部依赖联网核实：qwen3-vl-embedding（2560维，$0.258/M图）与 tongyi-embedding-vision-plus（1152维，$0.09）均存在；qwen3-rerank 有 OpenAI 兼容端点（gte-rerank-v2 已于 2026-05-30 下线）；MinerU v3.1 起改为 Apache 2.0 自定义协议、Windows/CPU 可用；Met API 免 key（限速 80 req/s，CC0 仅公共领域作品）。

### 发现的关键遗漏（方案未提）
1. **旧 PDF 实验遗产**：`src/data/pdf_loader.py` 是无引用孤儿代码（import fitz，但 PyMuPDF 不在 requirements.txt）；Chroma 里有残留 `user_pdfs` 集合；`data/uploads/` 有 12 个旧会话目录+真实 PDF；`data/index/` 有 faiss.index+metadata.pkl 残留（约49MB）。Stage 1 清理清单应加入。
2. **集成点**：AgentState 需加 `dataset_id` 字段；`web/service.py` stream_answer 的每轮状态重置清单需同步加。
3. README 仍引用已删除的 app.py（Gradio 兜底），requirements.txt 仍列 gradio。
4. MinerU 官方 known issue：艺术图册解析不佳——与项目领域直接相关，双路线设计可对冲但需真实画册验证。
5. MinerU CPU 同步阻塞 100 页 PDF 约 5-10 分钟，Phase 1 同步方案需限页数或提前做 BackgroundTasks。
6. 测试全是真实 API 冒烟测试，重构期应给 access.py 配纯单测；eval "完全一致"验收过严，LLM 有波动，应改"无显著回归"。
7. color_analysis 需 K-means，requirements 无 sklearn（可用 numpy/PIL 实现）。
8. Web UI 不宜作为落地顺序第 8 步整体推进，应拆进 Stage 3/5/6。

工作量粗估：全职约 2 周、兼职 3-4 周（Stage 3 最大头 4-6 天）。

---

## Stage 1 实施（同日，用户指令"进行 Agent 层清理"）

### 环境（重要，跨会话有效）
- **项目 Python 环境 = `C:\Users\86188\anaconda3\envs\artagent\python.exe`（3.10.20，用户当晚新建）**。旧 `llm` 环境（3.11.15）已弃用。PATH 里的 python、Python39、anaconda base 都没有项目依赖。
- ~~旧 llm 环境坑~~（已随换环境解决）：llm 环境里 sklearn+datasets+pyarrow 有 DLL 冲突导致 `import sentence_transformers` 段错误。新 artagent 环境**未装 datasets 包**，冲突链无法形成，导入正常。曾临时加的"pyarrow 先行导入"守护已移除（commit 1aaf48c）。**注意：若以后需在此环境安装 `datasets`（HF datasets）包，可能重新引入该冲突，装完须先测 `import sentence_transformers`。**
- 新环境已验证：依赖齐全（langchain 1.3.14/langgraph 1.2.10/chromadb 1.5.9/sentence-transformers 5.6.1/pyarrow 25.0.0）、BGE 离线缓存加载、Chroma 检索、全图构建均正常。
- 测试全走真实 LLM API（DashScope），全量验证链（test_access+test_tools+test_multi_tool+test_pipelines+eval）约 25-35 分钟。

### Stage 1 已完成（commit a08bda1）
- 新建 `src/data/access.py`（fuzzy_match 三级递进/row_to_artwork_dict/format_evidence_block/EVIDENCE_SNIPPET_LEN=200）+ `tests/test_access.py` 17 个纯单测全过。
- 工具 7→5：image_lookup 吸收 analyze_image（analyze 参数）；删 compare_artwork_styles（style_comparison.py）；query_painter_knowledge 去 LLM 化返回结构化统计。GENERAL_TOOLS 与 SYSTEM_PROMPT 同步。
- timeline 消除 iterrows 双重查询（gather 只产 row_to_artwork_dict，synthesize 用 format_evidence_block 拼文本）；comparison/recommendation/web_fallback 证据格式化统一。
- **顺手修复的隐藏 bug**：旧 `get_by_author` 按词序 contains，"Vincent van Gogh" 永远查空（AUTHOR 存 "GOGH, Vincent van"），fuzzy_match 分词匹配根治。
- 清理：faiss 残留索引（49MB）、README/README.en 的 app.py/Gradio 失效引用、requirements 移除 gradio。
- **`.gitignore` 的 `data/` 规则曾误伤 `src/data/` 源码**（pdf_loader.py 因此从未入库），已改为 `/data/` 只匹配根目录。

### 异常事件（待与用户确认）
- 工作期间发生**外部进程删除工作区文件**：整个 src/ 一度被删（已用 `git checkout -- src/` 恢复+重写丢失文件），随后 data/index/faiss.index+metadata.pkl 也被删（恰是待清理项）。pdf_loader.py 因未入库无法恢复（本就是待删遗产，无碍）。机器上可能有另一个 agent/同步进程在动这个目录，已提醒用户。

### 待办
- data/uploads/（12 个旧会话目录+真实 PDF）与 Chroma `user_pdfs` 集合：保留未动，Stage 3 重建用户 PDF 体系时处理。
- Stage 2 时：AgentState 加 dataset_id 字段 + web/service.py 的每轮状态重置清单同步。

### 实施方案文档（commit 39b0593）
- **`docs/ArtAgent-2.0-实施方案.md` 已生成**——原方案 + 评估 8 项补充 + Stage 1 实施记录的整合版，是 Stage 2-7 开发的唯一事实来源。后续新会话开工先读其 §1（当前状态）与 §13（陷阱速查）。
- 关键前置约定已写入文档：数据集/环境信息、各 Stage 集成点（dataset_id 入 state+service 清单、工具返回形状的消费者）、验收规约（纯单测+无显著回归 ≤2pp）、Web UI 拆进各 Stage、MinerU 画册短板与同步阻塞对策（Phase 1 即 BackgroundTasks）、多模态 embedding 成本选型（默认 tongyi-embedding-vision-plus）、color_analysis 用 numpy/PIL 不加 sklearn。
