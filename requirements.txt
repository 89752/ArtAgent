# Core LLM & Agent Framework
langchain>=0.3.0
langchain-openai>=0.2.0
langchain-community>=0.3.0
langgraph>=0.2.0
openai>=1.50.0

# Vector Store
chromadb>=0.5.0

# Embeddings
sentence-transformers>=3.0.0

# Data Processing
pandas>=2.0.0
numpy>=1.26.0

# Image Processing
Pillow>=10.0.0

# Web UI —— 自研前端（FastAPI + SSE），彻底替代 Gradio
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
# gradio 旧界面（app.py）保留作兜底，不再是主入口
gradio>=4.0.0

# Web Search (场景4兜底，可选：无 TAVILY_API_KEY 时优雅降级)
tavily-python>=0.5.0

# Utilities
python-dotenv>=1.0.0
tqdm>=4.66.0
pydantic>=2.0.0