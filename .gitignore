# ── Secrets（务必排除，切勿提交 API key） ──
# .env 存密钥与本地值；config.yaml 已入库且只含 $ENV 引用，不含密钥
.env

# ── 数据集与向量索引（体积大，不入库；README 说明如何获取/重建） ──
# 注意：/data/ 只匹配仓库根目录的数据目录；若写成 data/ 会把 src/data/ 里的
# 源代码也一并忽略（已踩坑：src/data/pdf_loader.py 曾因此游离在版本管理之外）。
SemArt/
/data/
*.db

# design docs: keep local only (user 2026-08-04)
/docs/

# 数据构建管线参与单元测试，需纳入版本管理；其余本地运维脚本继续忽略
/scripts/*
!/scripts/01_harvest_wikidata.py
!/scripts/02_build_extended_dataset.py
!/scripts/03_normalize_core.py
!/scripts/04_assemble_descriptions.py
!/scripts/05_audit_core.py
!/scripts/06_index_core.py
!/scripts/core_utils.py

# test fixture data files: keep local only (user 2026-08-04)
/tests/fixtures/

# deployment / generated / personal artifacts: keep local only (user 2026-08-04)
/Dockerfile
/docker-compose.yml
.dockerignore
eval/agent_eval_report.md
eval/results.md
eval/case_cache/

# ── Python ──
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
build/
dist/
.pytest_cache/
.mypy_cache/
.ruff_cache/

# ── 虚拟环境 ──
.venv/
venv/
env/

# ── 编辑器 / 系统 ──
.vscode/
.idea/
.DS_Store
Thumbs.db

# ── Gradio / 运行时产物 ──
*.log
flagged/
gradio_cached_examples/

# ── Node 前端（React + Vite） ──
node_modules/
/static/dist/
.pnpm-store/
