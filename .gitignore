# ── Secrets（务必排除，切勿提交 API key） ──
.env

# ── 数据集与向量索引（体积大，不入库；README 说明如何获取/重建） ──
# 注意：/data/ 只匹配仓库根目录的数据目录；若写成 data/ 会把 src/data/ 里的
# 源代码也一并忽略（已踩坑：src/data/pdf_loader.py 曾因此游离在版本管理之外）。
SemArt/
/data/
*.db

# design docs: keep local only (user 2026-08-04)
/docs/

# dev/ops helper scripts: keep local only (user 2026-08-04)
/scripts/

# ── AI 工具本地配置/产物（不入库） ──
.claude/
.agents/
.workbuddy/
skills-lock.json

# ── Python ──
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
build/
dist/
.pytest_cache/
.mypy_cache/
.ruff_cache/

# ── 虚拟环境 ──
.venv/
venv/
env/

# ── 编辑器 / 系统 ──
.vscode/
.idea/
.DS_Store
Thumbs.db

# ── Gradio / 运行时产物 ──
*.log
flagged/
gradio_cached_examples/
